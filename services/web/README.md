# Chiijie Outfit — Web Service

Lightweight Express app that serves a small frontend and proxies backend microservices.

Run locally with Docker Compose:

```bash
cd /workspaces/chiijie_outfit
docker compose up --build
```

Open http://localhost:3000
